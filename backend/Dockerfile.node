FROM node:22-alpine

WORKDIR /app

# backend/package.json をコピー（依存関係の分離済み）
COPY package.json ./
# package-lock.json があればコピーするが、現状ないので package.json のみ
RUN npm install --omit=dev

COPY . .

EXPOSE 5000

CMD ["node", "app.js"]
